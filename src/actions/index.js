import http from './http';

//getUsers.

export const getUsers = () => {
    return ( dispatch, getState ) => {
        //TODO Request users with axios.
        console.log( 'Requesting users ... ' );
    }
}